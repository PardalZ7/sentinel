{
  "name": "sentinel-usecase-webhook",
  "private": true,
  "workspaces": [
    "apps/*",
    "dashboard"
  ],
  "scripts": {
    "start": "cross-env LOCALSTACK_ENDPOINT=http://localhost:4566 concurrently \"npm run start --workspace=apps/webhook\" \"npm run start --workspace=dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^10.1.0"
  }
}
